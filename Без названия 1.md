Полная инструкция от точки «Этап 3 завершён» (созданы говорящие NPC) до готового квеста, с учётом интеграции вашей системы интеракции. Эта версия дополнительно включает создание объектов (дерево, тело Лобсанга, точки, актор состояния), которых в варианте «после этапа 4» уже предполагалось наличие.

Перед началом — два повторяющихся паттерна и две вставки чтения состояния; держите их под рукой.

**Блок P (диалог по инициативе игрока):**

```
[вход] → On Notify From Actor (Identity=<id>, Notify=<knock>)
            ├─ Success  → Notify Actor (Identity=<id>, Notify=<строка.NN>)
            └─ Completed → [следующий]
```

**Блок G (шаг сцены по инициативе графа):**

```
[вход] → Notify Actor (Identity=<актор>, Notify=<команда>)
       → On Notify From Actor (Identity=<актор>, Notify=<команда.Done>)
            └─ Success → [следующий]
```

**Чтение «есть ли предмет»:** `Get Player Pawn → Get Component by Class (AC_InteractionComponent) → Get CollectedItems → Has Tag (<Item-тег>)`. **Чтение флагов квеста:** `Get All Actors Of Class (BP_QuestState) → Get (index 0) → Get bSongKnown / bTextCopied`.

> **Что должно быть после Этапа 3:** все теги (включая `Monk.Item.*`, `Monk.State.*`, `Monk.Knowledge.*`, `Monk.System.Director.01`); говорящие NPC на `BP_NPC_Base` (Лхамо, Тензин, торговец, Долма); в `BP_InspectableBase` — переменная `ItemTag`; в `AC_InteractionComponent` — `CollectedItems` и запись тега при осмотре. Всё остальное создаём ниже.

---

# ЭТАП 4. Создать объекты-говорящие, актор состояния и точки

4.1. `BP_NPC_Base` → ПКМ → `Create Child Blueprint Class` → `BP_Obj_Tree`. Открыть → Flow-компонент → `Identity Tags` = `Monk.Object.Tree.01`. `Class Defaults` → `NPC Dialogue Tag` = `Monk.Event.Monastery.TreeCopy`. `Compile`/`Save`. Поставить на сцену (исполинское дерево у дома Долмы). 4.2. Так же `BP_Obj_Lobsang`: `Identity Tags` = `Monk.Object.Lobsang.01`, `NPC Dialogue Tag` = `Monk.Event.Monastery.Lobsang`. Поставить в ущелье. 4.3. Content Browser → ПКМ → `Blueprint Class` → `Actor` → `BP_QuestState`. Открыть → `Add Component` → Flow-компонент → выделить → `Identity Tags` = `Monk.System.QuestState.01`. `Compile`/`Save`. Поставить один экземпляр на сцену. (Переменные и логику добавим на Этапе 10.) 4.4. `Place Actors` → `Basic` → перетащить `Target Point` на сцену **у дерева** и второй **снаружи дома Долмы**.

---

# ЭТАП 5. Проверить запись осмотра в `AC_InteractionComponent`

Тег должен писаться при осмотре **любого** предмета (это фиксирует «знание» от пергамента и разбитой ступы), а не только collectable.

5.1. Откройте `AC_InteractionComponent` → функцию `HideExamineDetail`. 5.2. Если запись `ItemTag` у вас стоит внутри ветки `bCurrentItemIsCollectable = True` — перенесите её **в начало функции**, чтобы выполнялась всегда:

- `Cast to BP_InspectableBase` (Object = `LastInspectedActor`);
- `As BP Inspectable Base` → `Get ItemTag`;
- `Make Gameplay Tag Container from Tag` → `Append Gameplay Tag Containers` (A = `Get CollectedItems`, B = результат) → `Set CollectedItems`. 5.3. `Compile`, `Save`. (Пустой `ItemTag` ничего не добавит — для лор-объектов без тега безвредно.)

---

# ЭТАП 6. Хук условного «стука» в `BP_NPC_Base`

Лхамо, Дерево и целая ступа должны «стучать» в граф разными тегами по состоянию.

6.1. Откройте `BP_NPC_Base`. `Functions` → `+` → `GetQuestKnock`. В `Details` добавьте выходы: `UseQuestKnock` (Boolean) и `Knock` (Gameplay Tag). Тело по умолчанию — `Return Node` с `UseQuestKnock=false`. 6.2. Найдите место, где база при взаимодействии вызывает `Notify Graph (NPC Dialogue Tag)` (Этап 1). Перед вызовом вставьте: `GetQuestKnock → Branch(UseQuestKnock)`: `True` → `Notify Graph(Knock)`; `False` → `Notify Graph(NPC Dialogue Tag)`. 6.3. `Compile`, `Save`. Обычные NPC `GetQuestKnock` не трогают.

---

# ЭТАП 7. Лор-объекты как `BP_InspectableBase`

Колонны и пергаменты и разбитая ступа показывают текст через окно деталей; граф для них не нужен. Пергамент 2 и разбитая ступа несут «знание».

7.1. `BP_InspectableBase` → чайлд `BP_Insp_Column_01`. `Class Defaults`: `ItemMesh`, `ItemData.FullDescription` = текст знаков, `bIsCollectable`=false, `ItemTag`=пусто. Разместить. 7.2. Повторить:

|Ассет|FullDescription (суть)|ItemTag|Collectable|
|---|---|---|---|
|`BP_Insp_Column_02` / `_03`|другие надписи|пусто|false|
|`BP_Insp_Parchment_01`|духи двигали скалы и реки|пусто|false|
|`BP_Insp_Parchment_02`|разряд стержня ступы дал огонь Жинсрэг|`Monk.Knowledge.Parchment`|false|
|`BP_Insp_StupaRuined`|обожжённое место, разряженная ступа|`Monk.Knowledge.Ruined`|false|

7.3. Разместить по маршруту. Осмотр пергамента 2 и разбитой ступы автоматически кладёт «знание» в `CollectedItems`.

---

# ЭТАП 8. Подбираемые предметы как `BP_InspectableBase`

8.1. Чайлд `BP_Item_Brand`: `ItemMesh`, `ItemData` (ItemName «Тавро»), `bIsCollectable`=true, `ItemTag`=`Monk.Item.Brand`. Разместить у тела Лобсанга. **Не скрывать.** 8.2. Чайлд `BP_Item_Songsrang`: `bIsCollectable`=true, `ItemTag`=`Monk.Item.Songsrang`. `Event BeginPlay` → `Set Actor Hidden In Game`(true) → `Get InteractZone` → `Set Collision Enabled`(No Collision). Разместить у целой ступы. 8.3. Чайлд `BP_Item_Dictionary`: `bIsCollectable`=true, `ItemTag`=`Monk.Item.Dictionary`, скрытие в `BeginPlay` как в 8.2. Разместить у Лхамо.

---

# ЭТАП 9. Целая ступа (`BP_NPC_Base`, два состояния)

9.1. Чайлд `BP_Obj_StupaIntact`. Flow-компонент → `Identity Tags`=`Monk.Object.StupaIntact.01`. `Class Defaults` → `NPC Dialogue Tag`=`Monk.Dialogue.Monastery.StupaLocked`. 9.2. `Variables`: `bExtracted` (Boolean); `SongsrangActor` (Actor, `Instance Editable`). 9.3. Переопределите `GetQuestKnock`:

```
Branch(bExtracted)
  True  → UseQuestKnock=false
  False → Branch( Has Tag(Monk.Knowledge.Parchment) AND Has Tag(Monk.Knowledge.Ruined) )
            True  → Set Actor Hidden In Game(SongsrangActor,false)
                  → Get InteractZone(SongsrangActor) → Set Collision Enabled(Query Only)
                  → Set bExtracted=true
                  → UseQuestKnock=true, Knock=Monk.Event.Monastery.StupaExtract
            False → UseQuestKnock=false
```

9.4. `Compile`/`Save`. Разместить на возвратной развязке до дерева. В `Details` на сцене назначьте `SongsrangActor` пипеткой на `BP_Item_Songsrang`.

---

# ЭТАП 10. `BP_QuestState` — флаги и сохранение финала

10.1. Content Browser → ПКМ → `Blueprint Class` → `All Classes` → `SaveGame` → `SG_QuestState`. Переменная `Ending` (Name). `Compile`/`Save`. 10.2. Откройте `BP_QuestState`. `Variables`: `bSongKnown` (Boolean), `bTextCopied` (Boolean). 10.3. `Event BeginPlay` → возьмите Flow-компонент → привяжите обработчик приёма сигнала (как база на Этапе 1; это чистый актор) → `Switch on Gameplay Tag`:

- `Monk.State.Lhamo.SongKnown` → `Set bSongKnown=true`;
- `Monk.State.Tree.TextCopied` → `Set bTextCopied=true`;
- `Monk.State.Ending.Untie` → `Create Save Game Object(SG_QuestState)` → `Set Ending="Untie"` → `Save Game to Slot("QuestSave",0)`;
- `Monk.State.Ending.Burn` → то же с `"Burn"`. 10.4. `Compile`/`Save`.

---

# ЭТАП 11. Условная логика Лхамо и Дерева

11.1. `BP_NPC_Lhamo` → переопределите `GetQuestKnock`:

```
songKnown = (BP_QuestState).bSongKnown
hasBrand  = Has Tag(Monk.Item.Brand)
Branch(songKnown AND hasBrand)
  True  → UseQuestKnock=true, Knock=Monk.Action.Untie
  False → UseQuestKnock=false
```

11.2. `BP_Obj_Tree` → переопределите `GetQuestKnock`:

```
textCopied   = (BP_QuestState).bTextCopied
hasSongsrang = Has Tag(Monk.Item.Songsrang)
Branch(textCopied AND hasSongsrang)
  True  → UseQuestKnock=true, Knock=Monk.Action.Burn
  False → UseQuestKnock=false
```

11.3. `Compile`/`Save` обоих.

---

# ЭТАП 12. Режиссёр сцен `BP_CutsceneDirector`

12.1. Content Browser → ПКМ → `Blueprint Class` → `Actor` → `BP_CutsceneDirector`. `Add Component` → Flow-компонент → `Identity Tags`=`Monk.System.Director.01`. 12.2. `Variables` (ссылки `Instance Editable`): `LhamoRef`, `TreeRef`, `DolmaRef`, `DictionaryRef`, `LhamoWalkTarget`, `DolmaOutTarget` (Actor); `StartLoc`, `EndLoc` (Vector); `MovingActor` (Actor); `MoveDoneTag` (Gameplay Tag). 12.3. `Add Timeline` → `MoveTL` → `Float Track`, ключи (0→0),(2→1).

- `Update` → `Lerp(Vector)`(A=StartLoc,B=EndLoc,Alpha=трек) → `Set Actor Location`(MovingActor).
- `Finished` → `Notify Graph(MoveDoneTag)`.
- Функция `StartMove(Actor,TargetPoint,DoneTag)`: `Set MovingActor`; `Set StartLoc=GetActorLocation(Actor)`; `Set EndLoc=GetActorLocation(TargetPoint)`; `Set MoveDoneTag`; `MoveTL→Play from Start`. 12.4. `Event BeginPlay` → привяжите обработчик приёма сигнала → `Switch on Gameplay Tag`:

|Notify Tag|Действие|В конце|
|---|---|---|
|`Stage.Untie.WalkToTree`|`StartMove(LhamoRef,LhamoWalkTarget,Stage.Untie.WalkDone)`|(в Finished)|
|`Stage.Untie.Sing`|`Play Sound at Location`(LhamoRef) → `Delay 4`|`Notify Graph(Stage.Untie.SingDone)`|
|`Stage.Untie.Words`|показать `Lhamo.03` (ваш UI диалога или временно `Print String`) → `Delay 4`|`Notify Graph(Stage.Untie.WordsDone)`|
|`Stage.Untie.TreeVanish`|скрыть TreeRef (hidden+коллизия off) → `Delay 0.2`|`Notify Graph(Stage.Untie.TreeGone)`|
|`Stage.Untie.RevealDict`|показать DictionaryRef → `InteractZone Set Collision(Query Only)` → `Delay 0.2`|`Notify Graph(Stage.Untie.DictShown)`|
|`Stage.Untie.LhamoVanish`|скрыть LhamoRef → `Delay 0.2`|`Notify Graph(Stage.Untie.LhamoGone)`|
|`Stage.Untie.DolmaFree`|`StartMove(DolmaRef,DolmaOutTarget,Stage.Untie.DolmaFreeDone)`|(в Finished)|
|`Stage.Burn.TreeVanish`|скрыть TreeRef → `Delay 0.2`|`Notify Graph(Stage.Burn.TreeGone)`|
|`Stage.Burn.HouseCollapse`|(опц. звук) скрыть DolmaRef → `Delay 0.2`|`Notify Graph(Stage.Burn.HouseDone)`|
|`Stage.Burn.Words`|показать `ResolveBurn.01` → `Delay 4`|`Notify Graph(Stage.Burn.WordsDone)`|

12.5. `Compile`/`Save`. Поставить один на сцену; назначить все ссылки пипеткой (включая `LhamoWalkTarget` и `DolmaOutTarget` на точки из Этапа 4).

> Персонажи «скользят» к точкам — для прототипа нормально.

---

# ЭТАП 13. Заполнить `DT_Dialogues`

Только реплики говорящих акторов (лор показывают сами объекты). Для каждой: `Add` → `Tag` → реплики → ПКМ по `Tag` → `Copy` → имя строки = тег.

```
Monk.Dialogue.Monastery.Lhamo.01    первый разговор; подсказка про знак отца
Monk.Dialogue.Monastery.Lhamo.02    читает копию = её песня
Monk.Dialogue.Monastery.Lhamo.03    прощание: дар-словарь, просьба о матери
Monk.Dialogue.Monastery.Merchant.01
Monk.Dialogue.Monastery.Tenzin.01
Monk.Dialogue.Monastery.Dolma.01
Monk.Event.Monastery.Lobsang.01
Monk.Event.Monastery.TreeCopy.01
Monk.Dialogue.Monastery.StupaLocked.01   «внутри как будто что-то есть, открыть не получается»
Monk.Event.Monastery.StupaExtract.01     извлечение сонгсранга
Monk.Dialogue.Monastery.ResolveBurn.01   проход открыт, сонгсранг остаётся
```

---

# ЭТАП 14. Граф `FA_Monastery_01`: хребет, флаги, развилки

14.1. От `Start` разведите параллельно блоки P (один пин не даёт несколько проводов → поставьте узел-разветвитель):

|Блок P|Identity|On Notify|Notify Actor|
|---|---|---|---|
|Лхамо S1|`Monk.Characters.Lhamo.01`|`...Lhamo`|`...Lhamo.01`|
|Тензин|`Monk.Characters.Tenzin.01`|`...Tenzin`|`...Tenzin.01`|
|Торговец|`Monk.Characters.Merchant.01`|`...Merchant`|`...Merchant.01`|
|Долма|`Monk.Characters.Dolma.01`|`...Dolma`|`...Dolma.01`|
|Лобсанг|`Monk.Object.Lobsang.01`|`Monk.Event.Monastery.Lobsang`|`...Lobsang.01`|
|Копир. дерева|`Monk.Object.Tree.01`|`Monk.Event.Monastery.TreeCopy`|`...TreeCopy.01`|

14.2. **Ступа — закрыто (самоповтор).** Блок P: `Identity=Monk.Object.StupaIntact.01`, On Notify `Monk.Dialogue.Monastery.StupaLocked`, Notify Actor `...StupaLocked.01`. `Completed` → обратно во **вход того же** `On Notify From Actor` (реплика повторяется до открытия). 14.3. **Ступа — извлечение.** Блок P: On Notify `Monk.Event.Monastery.StupaExtract`, Notify Actor `...StupaExtract.01`. 14.4. **Лхамо S2.** От `Completed` блока **Копир. дерева** → блок P: `Identity=Monk.Characters.Lhamo.01`, On Notify `...Lhamo`, Notify Actor `...Lhamo.02`. 14.5. **Флаги:**

- `Completed` блока **Лхамо S2** → `Notify Actor`(`Monk.System.QuestState.01`, `Monk.State.Lhamo.SongKnown`).
- `Completed` блока **Копир. дерева** → `Notify Actor`(`Monk.System.QuestState.01`, `Monk.State.Tree.TextCopied`). 14.6. **Развилки от Start:**
- `On Notify From Actor`(`Monk.Characters.Lhamo.01`, `Monk.Action.Untie`) — слушатель расторжения.
- `On Notify From Actor`(`Monk.Object.Tree.01`, `Monk.Action.Burn`) — слушатель сожжения.

14.7. `Compile`, `Save`.

---

# ЭТАП 15. Граф: сцена расторжения

От `Success` слушателя расторжения — цепочка блоков G на `Monk.System.Director.01`:

```
WalkToTree→(WalkDone)→Sing→(SingDone)→Words→(WordsDone)
→TreeVanish→(TreeGone)→RevealDict→(DictShown)→LhamoVanish→(LhamoGone)
→DolmaFree→(DolmaFreeDone)
→ Notify Actor(Monk.System.QuestState.01, Monk.State.Ending.Untie) → Finish
```

`Compile`, `Save`.

---

# ЭТАП 16. Граф: сцена сожжения

От `Success` слушателя сожжения:

```
Burn.TreeVanish→(TreeGone)→Burn.HouseCollapse→(HouseDone)→Burn.Words→(WordsDone)
→ Notify Actor(Monk.System.QuestState.01, Monk.State.Ending.Burn) → Finish
```

`Compile`, `Save`.

> Любой `Finish` гасит второй слушатель; для строгой изоляции оберните ветви в `Sub Graph`.

---

# ЭТАП 17. Персонаж, тест, чек-лист

17.1. Убедитесь, что на персонаже подключён `AC_InteractionComponent` (раздел 9 вашей документации): компонент добавлен, заданы `InteractionIMC`/`GameplayIMC`, в `BeginPlay` добавлены gameplay-IMC, есть четыре делегирующих блока ввода.

17.2. **Тест, два отдельных прогона:**

- [ ] Осмотр колонн/пергаментов/разбитой ступы показывает текст; пергамент 2 и разбитая ступа считаются «прочитанными».
- [ ] Целая ступа до них — «закрыто» (повторяется); после — раскрывает `BP_Item_Songsrang`, который подбирается.
- [ ] Тавро подбирается у тела Лобсанга.
- [ ] Спина: Лхамо S1 → … → копирование дерева → Лхамо S2; после S2 `bSongKnown=true`, после копирования `bTextCopied=true` у `BP_QuestState`.
- [ ] **Расторжение:** имея тавро, после S2 поговорить с Лхамо → сцена 15, финал «Untie» сохранён, `Finish`.
- [ ] **Сожжение:** имея сонгсранг, после копирования взаимодействовать с деревом → сцена 16, финал «Burn», `Finish`.

17.3. Акт II: отдельный `Flow Asset`; NPC в `BeginPlay`: `Does Save Game Exist("QuestSave")` → `Load Game from Slot` → `Get Ending` → `Branch`: `Untie` → knock матери-с-яком; `Burn` → knock семьи-с-новорождённой.

Если что-то не сходится — назовите номер этапа и симптом, разберём точечно.